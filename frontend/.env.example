# 后端API地址
VITE_API_BASE_URL=http://localhost:8000

# 只允许浏览器可见的高德 Web端(JS API) Key；生产部署优先由后端运行时配置提供。
# 示例：VITE_AMAP_WEB_JS_KEY=replace-with-amap-browser-js-key
VITE_AMAP_WEB_JS_KEY=
# 安全码 VITE_AMAP_SECURITY_JS_CODE 配置在后端环境，经运行时接口提供。
# 模型、酒店、航班、高德 Web服务 Key 不得放进前端环境文件。
# 完整配置说明见 ../docs/API_KEYS.md。
